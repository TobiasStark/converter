# Binaries for the current project (executable file)
# If your project is named 'myproject', this ignores './myproject'
# or './myproject.exe'
**/*.exe
**/*.dll
**/*.so
**/*.dylib
**/*.o
**/*.a
**/*.wasm
**/*.d

# The Go build cache (can be large)
# Stored in $GOCACHE (defaults to $HOME/.cache/go-build)
# Only applies if 'go build' is run inside this directory
# (it usually is when using IDEs or standard build commands)
/go-build/

# Package files (e.g., generated by 'go test -c' or 'go test -i')
# Generally not needed for a standard project but good for completeness
/pkg/

# Dependency directories that may be created by older Go versions or tools
/vendor/

# Dependencies installed in the module cache (if not using Go Modules)
/bin/

# -------------------------------------------------------------------
# Environment/OS/Build Artifacts
# -------------------------------------------------------------------

# IDE/Editor temporary files (often ignored by general templates, but included here)
*~
\#*
.DS_Store # macOS Finder metadata

# Common temporary files and backup files
*.bak
*.swp
*.tmp

# -------------------------------------------------------------------
# Gin/Web Development Specific (Optional)
# -------------------------------------------------------------------

# Logs
# Gin applications often write logs. Keep them out of the repo.
*.log
/logs/

# Configuration files that hold sensitive environment variables
# (e.g., database credentials, API keys) that should not be committed.
# Always use environment variables in production.
.env
.env.*
config.local.json


# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
